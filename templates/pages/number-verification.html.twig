<!DOCTYPE html>
<html lang="fr">
<head>
    <title>Vérification de numéro de téléphone - Crousfy</title>

    <!-- Meta -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <meta name="description" content="Vérifiez votre numéro de téléphone pour sécuriser votre compte Crousfy">
    <meta name="author" content="Crousfy">
    <link rel="shortcut icon" href="favicon.ico">

    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#fefefe',
                            100: '#fdfdfd',
                            200: '#fafafa',
                            300: '#f5f5f5',
                            400: '#f0f0f0',
                            500: '#e8e8e8',
                            600: '#d1d1d1',
                            700: '#b3b3b3',
                            800: '#8c8c8c',
                            900: '#636363',
                        }
                    },
                    animation: {
                        'fade-in': 'fadeIn 0.5s ease-in-out',
                        'slide-up': 'slideUp 0.5s ease-out',
                        'pulse-slow': 'pulse 3s infinite',
                    }
                }
            }
        }
    </script>

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

    <style>
        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .card-modern {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .btn-modern {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            color: white;
            padding: 14px 28px;
            border-radius: 12px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }

        .btn-modern:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(102, 126, 234, 0.4);
        }

        .input-modern {
            border: 2px solid #e5e7eb;
            border-radius: 12px;
            padding: 16px 20px;
            font-size: 16px;
            transition: all 0.3s ease;
            background: #f9fafb;
        }

        .input-modern:focus {
            outline: none;
            border-color: #667eea;
            background: white;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .verification-code {
            letter-spacing: 0.5em;
            font-size: 24px;
            font-weight: bold;
            text-align: center;
        }
    </style>
</head>

<body class="min-h-screen bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50">
    <div class="min-h-screen flex items-center justify-center p-4" {{ stimulus_controller('number_verification', {seconds: seconds}) }}>
        <div class="w-full max-w-md">
            <!-- Logo et titre -->
            <div class="text-center mb-8">
                <div class="w-20 h-20 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg">
                    <i class="fas fa-shield-check text-white text-2xl"></i>
                </div>
                <h1 class="text-3xl font-bold text-gray-800 mb-2">Vérification</h1>
                <p class="text-gray-600">Sécurisez votre compte</p>
            </div>

            <!-- Card principale -->
            <div class="card-modern p-8">
                <!-- Messages flash -->
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="mb-6 p-4 rounded-lg {% if label == 'success' %}bg-green-50 border border-green-200 text-green-800{% elseif label == 'error' %}bg-red-50 border border-red-200 text-red-800{% else %}bg-blue-50 border border-blue-200 text-blue-800{% endif %}">
                            <div class="flex items-center">
                                <i class="fas {% if label == 'success' %}fa-check-circle{% elseif label == 'error' %}fa-exclamation-circle{% else %}fa-info-circle{% endif %} mr-3"></i>
                                {{ message }}
                            </div>
                        </div>
                    {% endfor %}
                {% endfor %}

                <!-- Numéro de téléphone -->
                <div class="text-center mb-6">
                    <div class="flex items-center justify-center mb-4">
                        <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mr-3">
                            <i class="fas fa-phone text-blue-600"></i>
                        </div>
                        <div class="text-left">
                            <p class="text-sm text-gray-600">Numéro de téléphone</p>
                            <p class="font-semibold text-gray-800">{{ number ?? "" }}</p>
                        </div>
                    </div>
                </div>

                <!-- Message de statut -->
                <div class="mb-6 text-center">
                    {% if seconds == 0 %}
                        <div class="p-4 bg-red-50 border border-red-200 rounded-lg" {{ stimulus_target('number_verification', 'counterParent') }}>
                            <i class="fas fa-exclamation-triangle text-red-500 mr-2"></i>
                            <span class="text-red-700 font-medium">Le code de vérification a expiré</span>
                        </div>
                    {% else %}
                        <div class="p-4 bg-blue-50 border border-blue-200 rounded-lg" {{ stimulus_target('number_verification', 'counterParent') }}>
                            <i class="fas fa-clock text-blue-500 mr-2"></i>
                            <span class="text-blue-700">Le code expire dans: <strong {{ stimulus_target('number_verification', 'counter') }} class="text-blue-800">--:--</strong></span>
                        </div>
                    {% endif %}
                </div>

                <!-- Formulaire -->
                <div class="space-y-6">
                    {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-key mr-2"></i>
                            Code de vérification
                        </label>
                        <div class="relative">
                            {{ form_widget(form.code, {
                                'attr': {
                                    'class': 'input-modern w-full verification-code',
                                    'placeholder': '000000',
                                    'maxlength': '6',
                                    'autocomplete': 'off'
                                }
                            }) }}
                            <div class="absolute inset-y-0 right-0 pr-3 flex items-center">
                                <i class="fas fa-mobile-alt text-gray-400"></i>
                            </div>
                        </div>
                        {% if form.code.vars.errors %}
                            <div class="mt-2 text-red-600 text-sm">
                                {% for error in form.code.vars.errors %}
                                    <i class="fas fa-exclamation-circle mr-1"></i>
                                    {{ error.message }}
                                {% endfor %}
                            </div>
                        {% endif %}
                    </div>

                    <button type="submit" class="btn-modern w-full">
                        <i class="fas fa-check mr-2"></i>
                        Vérifier le code
                    </button>

                    {{ form_end(form) }}
                </div>

                <!-- Lien de renvoi -->
                <div class="mt-6 text-center">
                    <a href="{{ path('user.verification.resend') }}" class="text-blue-600 hover:text-blue-800 font-medium text-sm transition-colors duration-200">
                        <i class="fas fa-redo mr-1"></i>
                        Renvoyer le code
                    </a>
                </div>
            </div>

            <!-- Footer -->
            <div class="text-center mt-8">
                <p class="text-sm text-gray-500">
                    <i class="fas fa-shield-alt mr-1"></i>
                    Votre sécurité est notre priorité
                </p>
            </div>
        </div>
    </div>
</body>
</html>

